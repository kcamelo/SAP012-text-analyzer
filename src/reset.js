import { button } from './index.js';

function reset() { }
-button();
{
    // Obtém a referência da caixa de texto pelo ID
    var caixaDeTexto = document.getElementById('reset-button');

    // Limpa o conteúdo da caixa de texto
    caixaDeTexto.value = '';
}
